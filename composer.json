{
    "name": "vshred/fb-pixel-replay",
    "description": "Facebook Pixel Event Replay Tool - Backfill historical purchase events to Facebook Conversions API",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Luis B. Mata",
            "email": "mataluis2k@gmail.com"
        }
    ],
    "require": {
        "php": ">=8.0",
        "guzzlehttp/guzzle": "^7.5",
        "league/csv": "^9.8"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },

    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "scripts": {
        "test": "phpunit",
        "install-deps": "composer install --no-dev --optimize-autoloader"
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
